server:
  port: 9401
spring:
  profiles:
    active: dev
  application:
    name: micro-oauth2-auth
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8849
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  redis:
    database: 18
    port: 6379
    host: 101.132.25.75
    password: vevor123456
management:
  endpoints:
    web:
      exposure:
        include: "*"
